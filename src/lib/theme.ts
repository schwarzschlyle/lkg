/**
 * The one global class in the codebase. Applying it to a section re-declares
 * every design token for warm-bone light mode and applies the chevron seam
 * clip (spec §1.1 / §1.1b). Defined in `src/styles/tokens.css`.
 */
export const LIGHT_SECTION_CLASS = 'section--light';
